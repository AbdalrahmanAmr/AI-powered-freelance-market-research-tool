# ── Always required ──────────────────────────────────────
playwright>=1.59.0
python-dotenv>=1.0.0        # loads .env automatically

# ── Install only the model(s) you use ───────────────────

# Gemini free tier
google-genai>=1.0.0

# Groq free tier (fast LLaMA 3.3)
groq>=0.9.0

# OpenRouter free tier (uses OpenAI-compatible SDK)
openai>=1.30.0

# Claude paid API
# anthropic>=0.25.0